# Required for JWT signing in the API container.
JWT_KEY=replace-with-a-development-secret-at-least-32-characters

# Optional Stripe settings. Payments remain disabled unless explicitly enabled.
STRIPE_ENABLED=false
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
